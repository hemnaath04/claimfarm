# Copy these into the Alibaba Function Compute 3.0 console
# (Function detail page -> Configurations -> Environment Variables -> Edit).
# Each line is KEY=VALUE; replace the right-hand side with the live secret
# from ~/projects/claimfarm/.env. Do NOT commit a populated copy.

# ---------- Qwen Cloud ----------
QWEN_API_KEY=
QWEN_BASE_URL=https://dashscope-intl.aliyuncs.com/compatible-mode/v1
QWEN_VL_MODEL=qwen-vl-max
QWEN_CHAT_MODEL=qwen-max
QWEN_EMBED_MODEL=text-embedding-v3

# ---------- Alibaba Cloud ----------
ALIBABA_ACCESS_KEY_ID=
ALIBABA_ACCESS_KEY_SECRET=
ALIBABA_REGION=ap-southeast-1

# ---------- OSS ----------
OSS_BUCKET=claimfarm-files
OSS_ENDPOINT=https://oss-ap-southeast-1.aliyuncs.com

# ---------- DashVector ----------
VECTOR_STORE=dashvector
DASHVECTOR_API_KEY=
DASHVECTOR_ENDPOINT=vrs-sg-b0q4uc3th0001u.dashvector.ap-southeast-1.aliyuncs.com
DASHVECTOR_CLUSTER=claimfarm

# ---------- Open-Meteo (no key needed) ----------
OPEN_METEO_BASE=https://archive-api.open-meteo.com/v1/archive

# ---------- Telegram ----------
TELEGRAM_BOT_TOKEN=
TELEGRAM_API_BASE=https://api.telegram.org
# High-entropy string passed to setWebhook as secret_token (SEC-002)
TELEGRAM_WEBHOOK_SECRET=

# ---------- Bird ----------
# HMAC-SHA256 signing secret from the Bird webhook panel (SEC-003)
BIRD_WEBHOOK_SECRET=

# ---------- FastAPI / app ----------
DATABASE_URL=sqlite:////tmp/claimfarm.sqlite
PUBLIC_BASE_URL=https://claimfarm-api-wovsxktpbk.ap-southeast-1.fcapp.run
FRONTEND_BASE_URL=https://claimfarm-dashboard.vercel.app
LOG_LEVEL=INFO
LISTEN_PORT=9000

# Comma-separated; the regex in app/api_claims.py also auto-allows
# *.vercel.app preview deployments, so this list is for the canonical
# production URLs.
CORS_ALLOWED_ORIGINS=https://claimfarm-dashboard.vercel.app,http://localhost:3000

# ---------- Email transport (optional) ----------
# Leave RESEND_API_KEY blank to keep dev-mode click-through links in
# the JSON response. Set it to enable real email delivery via Resend.
# Branded HTML emails (verify/reset/magic-link/invite/claim-PDF) send through
# this. Use a verified-domain sender; RESEND_REPLY_TO improves deliverability.
RESEND_API_KEY=
RESEND_FROM=ClaimFarm <noreply@hemnaath.tech>
RESEND_REPLY_TO=help@hemnaath.tech

# ---------- Worker tuning (optional, defaults shown) ----------
JOB_BACKEND=thread
JOB_MAX_WORKERS=4

# ---------- Observability (optional) ----------
SENTRY_DSN=
SENTRY_ENVIRONMENT=production
